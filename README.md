# ietf pydantify Presentation

The presentation is built with love and LaTex Beamer


## Requirments

For minted `pip install Pygments` needs to be installed and `Latex-workshop>Latex>Magic:Args` need to have `-shell-escape`.